#include <iostream>
using namespace std;

void solve() {
    int n,k;cin >> n >> k;
    if(k <=n) {cout << 1 << endl;}
    int ans{1};
    k -= n;
    for(int i{n-1};i>=1;i--) {
        
    }
}

int main() {
    int t;cin >> t;
    while(t--) {
        solve();
    }
}